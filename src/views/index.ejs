<%- include('partials/header') %>
<body>
    <%- include('partials/navbarIndex') %>
    <br>
   <div class="container">
    <div class="jumbotron">
        <h1 class="display-4">Teck Blog</h1>
        <p class="lead">As principais noticias de Tecnologia e inovação você encontra aqui. Uma equipe seriamente comprometida em trazer o que ha de melhor neste mundo Tec! Seja muito bem vindo.</p>
        <hr class="my-4">
        <p>Deseja saber mais? Clique abaixo e fique por dentro dos nossos bastidores.</p>
        <p class="lead">
          <a  class="btn btn-outline-dark" href="#" role="button">Saber mais</a>
        </p>
      </div>
      <hr>
      <% response.forEach(article => { %>
            <div class="card">
                <div class="card-header">
                    <%= article.title %> 
                </div>
                <div class="card-body">
                    <%= article.article %>
                </div>
            </div>
      <% }) %>

   </div>
</body>
<%- include('partials/footer') %>